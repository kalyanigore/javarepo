create database gmail;
use gmail;

create table login(username varchar(255),password varchar(255));

create table inbox(username varchar(255),messages varchar(255));

create table draft(username varchar(255),messages varchar(255));

